export default async function handler(req,res){
 if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {amount,customer,design,returnUrl}=req.body||{};
 if(!amount||!customer?.email||!customer?.name||!design?.width)return res.status(400).json({error:'Datos incompletos'});
 const token=process.env.MERCADOPAGO_ACCESS_TOKEN;const external=`gate_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;
 if(!token){const u=new URL(returnUrl||`https://${req.headers.host}/`);u.searchParams.set('payment','demo-approved');u.searchParams.set('ref',external);return res.status(200).json({mode:'demo',demo_url:u.toString(),external_reference:external});}
 const base=returnUrl||`https://${req.headers.host}/`;const preference={items:[{id:'technical-visit',title:'Reserva visita técnica — reja a medida',quantity:1,currency_id:'CLP',unit_price:Number(amount)}],payer:{email:customer.email,name:customer.name},external_reference:external,back_urls:{success:`${base}?payment=success`,failure:`${base}?payment=failure`,pending:`${base}?payment=pending`},auto_return:'approved',notification_url:process.env.MERCADOPAGO_WEBHOOK_URL||undefined,statement_descriptor:'FERROFORMA',metadata:{commune:customer.commune||'',width:String(design.width),height:String(design.height),style:design.style||'',material:design.material||''}};
 const r=await fetch('https://api.mercadopago.com/checkout/preferences',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(preference)});const d=await r.json();if(!r.ok)return res.status(r.status).json({error:d.message||'Mercado Pago rechazó la solicitud'});return res.status(200).json({mode:'production',init_point:d.init_point,id:d.id,external_reference:external});
}
